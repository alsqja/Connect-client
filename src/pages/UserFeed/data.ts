export interface Post {
  id: number;
  url: string;
  description: string;
  createdAt: string;
}

export interface IFeedProfile {
  id: number;
  name: string;
  birth: string;
  gender: string;
  profileUrl: string;
}
